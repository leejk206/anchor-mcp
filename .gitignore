node_modules/
dist/
*.tsbuildinfo
.env
.env.*
*.log
.DS_Store

# internal working docs (not part of the public project)
GRANT.md
PLAN.md
.superstack/

# AI session export (proof-of-work; contains full transcript — never commit)
*-session.jsonl
claude-session.jsonl

# AI subscription receipts (contain PII: name/home address/email — final-tranche proof, never commit)
Receipt-*.pdf
*:Zone.Identifier

# secrets (npm 2FA recovery codes, tokens, etc. — NEVER commit)
.secret
.secret.*
.secrets/

# skill installer artifacts (npx skills add)
.agents/
skills-lock.json
